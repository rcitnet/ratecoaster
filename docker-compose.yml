services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: parkpulse
      POSTGRES_PASSWORD: parkpulse
      POSTGRES_DB: parkpulse
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U parkpulse"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  pgdata:
